{
  "_comment": "COUPLING tier-2, the flagship pair. The sides table gives 4C an unstarred yes in BOTH columns ('coupled to FEniCSx, both roles') and the 4C payload notes this contradicts the deprecated coupled_solve docstring, which lists 4C on the Neumann side only. One of the two statements about 4C in this repo is wrong and nothing decided which; this fixture runs 4C on the Dirichlet side, on the Neumann side, and in the right subdomain where its outward normal points the other way. It is also the pair with the most ways to converge to a wrong answer, all of them listed in the payload: a spatially varying 4C boundary datum is VAL x FUNCT with FUNCT a symbolic expression fitted by least squares, so a bad fit is a silently wrong BC and not an error; VTU output is scatra-<step>-<rank>.vtu and sorting on the trailing RANK returns the all-zero initial condition, which reads as a converged solve of a trivial problem; a QUAD4 VTU repeats every node once per element, so a participant that does not collapse duplicates exports four times too many points. Each produces a run that converges. The closed form is what separates them. 4C's participant is a WRAPPER \u2014 `command` runs plain Python with numpy and meshio and the binary path goes into FOURC_BIN inside the script \u2014 which is the distinction four dead .replace() calls used to drop from the served launch text.",
  "_mutation": {
    "note": "Configure both participants with a conductivity 25% away from the one the closed form is computed from. The coupling stays SELF-CONSISTENT: it converges, the two sides still agree at the interface, and the flux balance is untouched because both subdomains moved together \u2014 the interface temperature does not even change, since the conductance RATIO is preserved. Only the interface FLUX moves, by the same 25%. So this is a converged, balanced, internally unimpeachable coupling of the wrong problem, which is the exact silent-wrong shape these fixtures exist to catch, and the q_err check is the only thing that sees it. Run by hand against the subject rather than the setup, the equivalent was flipping the sign of `normal_fluxes` in data/coupling_participants/participant_skfem.py (not stageable, so not declarable here): that produced a run which converged in 62 iterations and balanced to 3.9e-08 while landing 22 K from the exact interface temperature.",
    "file": "../_lib/couplinglib.py",
    "from": "\"IFACE_X\": f\"{problem.xi}\", \"K\": f\"{k}\", \"F_SRC\": \"0.0\",",
    "to": "\"IFACE_X\": f\"{problem.xi}\", \"K\": f\"{1.25 * k}\", \"F_SRC\": \"0.0\","
  },
  "backend": "coupling",
  "physics": "pair_fenics_fourc",
  "pitfall_index": 3,
  "mode": "python",
  "timeout_seconds": 1800,
  "expect_in_output": [
    "fourc_D_fenics_N_converged=True",
    "fourc_D_fenics_N_meshes_nonmatching=True",
    "fourc_D_fenics_N_interface_T_matches_reference=True",
    "fourc_D_fenics_N_interface_q_matches_reference=True",
    "fourc_D_fenics_N_flux_signs_are_opposite=True",
    "fourc_D_fenics_N_flux_balanced=True",
    "fourc_D_fenics_N_validation_empty=True",
    "fourc_D_fenics_N_matches_monolithic=True",
    "fenics_D_fourc_N_converged=True",
    "fenics_D_fourc_N_interface_q_matches_reference=True",
    "fenics_D_fourc_N_matches_monolithic=True",
    "fenics_N_fourc_D_converged=True",
    "fenics_N_fourc_D_interface_T_matches_reference=True",
    "fenics_N_fourc_D_matches_monolithic=True",
    "pairs_run=3",
    "failures_count=0"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:",
    "TIMEOUT",
    "did_not_converge",
    "_meshes_nonmatching=False",
    "_interface_T_matches_reference=False",
    "_interface_q_matches_reference=False",
    "_flux_signs_are_opposite=False",
    "_flux_balanced=False",
    "_validation_empty=False",
    "_matches_monolithic=False",
    "failures_count=1"
  ]
}
