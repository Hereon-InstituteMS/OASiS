{
  "_comment": "CLAIM contact::2: [API] Use AffineConstraints to enforce contact constraints. Signal: hand-modifying the system matrix (zeroing rows + setting diag = 1) for constrained nodes is brittle and breaks parallel assembly. AffineConstraints<double> with constraints.add_line + add_entries handles the matrix modifications \u2026 WHAT THIS RUNS: -- probe \"contact_handmade_rows\" of the shared contact translation unit _shared/contact_family.cc, compiled once and cached so three fixture directories share ONE C++ build. One CONVERGED active set from the iterated loop, imposed two ways on the same matrix, so the only thing that varies is how the constraint is applied: the hand-written route zeroes each constrained ROW and puts 1 on the diagonal, and AffineConstraints eliminates the constrained COLUMNS into the right-hand side as well. The entry calls the hand-written route \"brittle\". Measured, brittle means this: the operator stops being symmetric (relative symmetry defect 0.163), and CG -- the solver this SPD problem calls for -- runs its whole 3000-iteration budget and returns with a residual of 113. This is one of the LOUD failures: a direct solve of the same unsymmetric system returns the AffineConstraints answer to 2e-16, so the constraint itself was imposed correctly. What the missing column elimination costs is the property the solver needs, not the answer. The entry's parallel-assembly claim is NOT tested: this deal.II is built without MPI. Mutation control: T2_MUTATE=1 eliminates the columns too, the operator is symmetric to machine precision, CG converges, and the fixture fails its own expectations. OBSERVED, copied from the run: enforcement_under_test=hand_modified_rows; operator_under_test_is_symmetric=false; cg_converged=false; solution_under_test_matches_the_affine_constraints_one=true; VERDICT=hand_modified_rows_destroy_the_symmetry. Verified by execution 2026-08-06 against deal.II 9.8.0-pre (Release at ~/dealii/build; Debug where the fixture sets requires_debug); every expect_in_output string above is text the software actually printed. T2_MUTATE=1 runs the correct variant instead, and the fixture then fails its own expectations \u2014 checked, so it cannot pass with the pathology absent.",
  "backend": "dealii",
  "physics": "contact",
  "pitfall_index": 2,
  "mode": "cmd",
  "expect_in_output": [
    "enforcement_under_test=hand_modified_rows",
    "operator_under_test_is_symmetric=false",
    "cg_converged=false",
    "solution_under_test_matches_the_affine_constraints_one=true",
    "VERDICT=hand_modified_rows_destroy_the_symmetry"
  ],
  "forbid_in_output": [
    "BUILD_FAILED",
    "UNKNOWN_PROBE",
    "VERDICT=enforcement_under_test_keeps_the_operator_symmetric"
  ],
  "timeout_seconds": 1800
}
