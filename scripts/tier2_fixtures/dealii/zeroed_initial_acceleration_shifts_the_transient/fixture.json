{
  "_comment": "Tier-2 for dealii wave#3. Tier-2 for dealii wave#3 \u2014 probe \"zero_initial_acceleration\" of the shared wave translation unit _shared/wave_family.cc, compiled once and cached so six fixtures share one build. Two integrations of the same problem, same mesh, same dt, in one process: one starts from a_0 = 0, the other from the a_0 that solves M a_0 = -c^2 K u_0. a_0 is the only difference, so the deviation between them IS the pitfall. The entry's magnitudes are measured, not repeated: it promises an O(1) error in the first ten steps decaying to O(h^p) later, and a superimposed mode of magnitude 0.1-0.3. What this run shows is a deviation of a few percent that does NOT decay \u2014 the fixture pins both of those as false. Mutation control: T2_MUTATE=1 makes the probe run the CORRECT variant (solve for a_0), the two integrations become identical, and the fixture then fails its own expectations. Verified by execution 2026-08-06 against deal.II 9.8.0-pre (Release at ~/dealii/build; Debug at the dbgbuild tree where the fixture sets requires_debug); every expect_in_output string was copied from that run.",
  "backend": "dealii",
  "physics": "wave",
  "pitfall_index": 3,
  "mode": "cmd",
  "expect_in_output": [
    "early_deviation_is_measurable=true",
    "initial_acceleration_solved=false",
    "early_deviation_is_of_order_one=false",
    "deviation_decays_at_later_times=false",
    "spurious_mode_reaches_the_claimed_0.1_to_0.3_magnitude=false",
    "VERDICT=zeroing_the_initial_acceleration_moves_the_solution"
  ],
  "forbid_in_output": [
    "BUILD_FAILED",
    "UNKNOWN_PROBE",
    "VERDICT=initial_acceleration_choice_left_no_trace"
  ],
  "timeout_seconds": 900
}
