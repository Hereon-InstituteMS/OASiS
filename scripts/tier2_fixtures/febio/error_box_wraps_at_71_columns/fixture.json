{
  "_comment": "Tier-2 for febio::linear_elasticity#9. Asserts a NEGATIVE: the sentence 'The selected linear solver does not support the requested matrix format.' is NOT present as a fixed string in the captured output of the run that printed exactly that error, because the ERROR box breaks the line after \"matrix\"; the fragment 'does not support the requested' is, and the whole sentence becomes findable only after whitespace-unwrapping. Also separates the neighbouring mistake: <symmetric_stiffness>unsymmetric</symmetric_stiffness> is rejected at parse by name. Positive control: the same deck with 'symmetric' runs clean and prints no fragment. Executed 2026-08-06 on FEBio 4.12.0.86045466d (source build, USE_MKL=OFF). MUTATION CONTROL (2026-08-07): T2_MUTATE=1 tests the whole-sentence grep against the UNWRAPPED capture, i.e. the world in which FEBio's ERROR box does not break the line after \"matrix\". Unmutated the fixture still PASSES; mutated it FAILS because the expectation 'error_box_wrap=reproduced' is no longer printed and the forbidden token 'not_reproduced' appears. Re-verify with `cd <fixture dir> && T2_MUTATE=1 ~/Schreibtisch/open-fem-agent/.venv/bin/python source.py` (the mutation branch lives in source.py, so the proof is one command and needs no external harness).",
  "backend": "febio",
  "physics": "linear_elasticity",
  "pitfall_index": 9,
  "mode": "python",
  "timeout_seconds": 240,
  "expect_in_output": [
    "error_box_wrap=reproduced"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:",
    "not_reproduced",
    "skipped"
  ]
}
