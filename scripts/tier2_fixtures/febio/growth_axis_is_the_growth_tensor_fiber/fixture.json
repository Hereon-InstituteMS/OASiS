{
  "_comment": "Tier-2 for febio::growth_remodeling#3, syntax half. <mat_axis> and the old <theta> spelling on a `kinematic growth` material each give 'tag \"<name>\" (line N) : unrecognized tag'; <MeshData> placed before </MeshDomains> gives 'MeshData must appear after MeshDomain section. (line N)', which is NOT a tag-family message \u2014 the fixture asserts that too, because a wrapper matching only the tag family will not recognise it. The default-axis half of the claim is a geometric measurement and is named as uncovered. Positive control: the shipped template, unmodified, which reaches normal termination. Executed 2026-08-06 on FEBio 4.12.0.86045466d (source build, USE_MKL=OFF). MUTATION CONTROL (2026-08-07): T2_MUTATE=1 neutralises the L.swap / L.drop edits this fixture uses to turn a correct deck into a broken one, so every deck it builds is the correct one and the pitfall is never triggered. Unmutated the fixture still PASSES; mutated it FAILS because the expectation 'growth_axis_names=reproduced' is no longer printed and the forbidden token 'not_reproduced' appears. Re-verify with `cd <fixture dir> && T2_MUTATE=1 ~/Schreibtisch/open-fem-agent/.venv/bin/python source.py` (the mutation branch lives in source.py, so the proof is one command and needs no external harness).",
  "backend": "febio",
  "physics": "growth_remodeling",
  "pitfall_index": 3,
  "mode": "python",
  "timeout_seconds": 600,
  "expect_in_output": [
    "growth_axis_names=reproduced"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:",
    "not_reproduced",
    "skipped"
  ]
}
