{
  "_comment": "Tier-2 for febio::hyperelasticity#5. <max_ups> flat inside <solver> and inside <qn_method type=\"JFNK\"> both give 'tag \"max_ups\" (line N) : unrecognized tag'; inside <qn_method type=\"BFGS\"> it is accepted and the deck runs to normal termination; and an unregistered strategy name gives 'tag \"qn_method\" (line N) : invalid value for attribute \"type\"' and NOT the max_ups message. All four required. Executed 2026-08-06 on FEBio 4.12.0.86045466d (source build, USE_MKL=OFF). MUTATION CONTROL (2026-08-07): T2_MUTATE=1 neutralises the L.swap / L.drop edits this fixture uses to turn a correct deck into a broken one, so every deck it builds is the correct one and the pitfall is never triggered. Unmutated the fixture still PASSES; mutated it FAILS because the expectation 'max_ups_placement=reproduced' is no longer printed and the forbidden token 'not_reproduced' appears. Re-verify with `cd <fixture dir> && T2_MUTATE=1 ~/Schreibtisch/open-fem-agent/.venv/bin/python source.py` (the mutation branch lives in source.py, so the proof is one command and needs no external harness).",
  "backend": "febio",
  "physics": "hyperelasticity",
  "pitfall_index": 5,
  "mode": "python",
  "timeout_seconds": 240,
  "expect_in_output": [
    "max_ups_placement=reproduced"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:",
    "not_reproduced",
    "skipped"
  ]
}
