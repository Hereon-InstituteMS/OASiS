{
  "_comment": "Tier-2 for fenics linear_elasticity#0: Vector function space for elasticity must use ('Lagrange', 1, (gdim,)). Passing plain ('Lagrange', 1) gives a SCALAR space; the elasticity weak form fails at form construction (ufl.sym(ufl.grad(u)) on scalar u) with ValueError 'Symmetric part of tensor with rank != 2 is undefined.'. Verified empirically 2026-06-01. MUTATION CONTROL (T2_MUTATE=1, re-runnable): with the vector space ('Lagrange', 1, (2,)) the probe prints 'trial_shape=(2,)' and 'sym_grad_accepted=True strain_shape=(2, 2)', so the expectations 'ValueError' and 'Symmetric part of tensor with rank != 2' both go missing. Proved KILLED 2026-08-07.",
  "backend": "fenics",
  "physics": "linear_elasticity",
  "pitfall_index": 0,
  "mode": "python",
  "expect_in_output": [
    "ValueError",
    "Symmetric part of tensor with rank != 2"
  ],
  "forbid_in_output": [],
  "_mutation": {
    "note": "Antidote: build the space the catalog actually recommends, ('Lagrange', 1, (gdim,)). sym(grad(u)) is then a legal rank-2 expression, nothing raises, and the probe prints the strain shape.",
    "file": "source.py",
    "from": "MUTATE = os.environ.get(\"T2_MUTATE\") == \"1\"",
    "to": "MUTATE = True"
  }
}
