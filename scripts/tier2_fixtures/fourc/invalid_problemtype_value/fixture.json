{
  "_comment": "Tier-2: 4C 2026.3.0-dev rejects an invalid value for PROBLEMTYPE inside PROBLEM TYPE section. The error from core/io/src/4C_io_input_spec_builders.cpp says 'Could not match this input' and echoes the offending YAML block. Useful for an agent that has to compose YAML — catches mis-typed problem types early. Verified empirically 2026-06-01. MUTATION CONTROL (T2_MUTATE=1, re-runnable): PROBLEMTYPE is set to 'Structure' instead of 'TotallyMadeUpProblem'. All three expectations - 'Could not match this input', 'PROBLEMTYPE', 'input_spec_builders' - disappear, because 4C parses the deck and proceeds to fill_complete(). Proved KILLED 2026-08-07.",
  "backend": "fourc",
  "physics": "input_format",
  "pitfall_index": 6,
  "mode": "cmd",
  "expect_in_output": [
    "Could not match this input",
    "PROBLEMTYPE",
    "input_spec_builders"
  ],
  "forbid_in_output": [],
  "_mutation": {
    "note": "remove the pathology: name a REAL problem type (Structure). The input spec then matches, no 'Could not match this input' block is echoed, and the fixture must go red.",
    "file": "cmd.sh",
    "from": "MUTATE=\"${T2_MUTATE:-0}\"",
    "to": "MUTATE=1"
  }
}
