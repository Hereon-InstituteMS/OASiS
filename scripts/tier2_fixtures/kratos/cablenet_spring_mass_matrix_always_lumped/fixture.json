{
  "_comment": "Tier-2: EmpiricalSpringElement3D2N's CalculateMassMatrix unconditionally fills only the diagonal — there is no consistent-mass path, and setting COMPUTE_LUMPED_MASS_MATRIX has zero effect for this element. Pitfall (kratos.cable_net #5). Executed on Kratos 10.4.3 (pip wheels, glibc-2.31 host, fresh venv on external ext4 image). Measured on a 1 m spring, A=1e-4, rho=7850: the 6x6 mass matrix has max|off-diagonal| EXACTLY 0.0 with a non-zero diagonal, and it is still exactly 0.0 after setting ProcessInfo[COMPUTE_LUMPED_MASS_MATRIX] to False, which is the knob that would request a consistent matrix. The flag being ignored is the defect; the element raises nothing. The fixture imports KratosMultiphysics at module scope; with Kratos absent the import raises, the process exits non-zero and the runner records a failure, never a pass. MUTATION CONTROL (added 2026-08-06, re-runnable): T2_MUTATE=1 INVERTS the expected outcome of all 4 probes -- it asserts the opposite of what this build actually does, while leaving each probe's callable untouched so every probe still really runs against Kratos. Every probe[...] line then disagrees with itself and probe_mismatches goes from 0 to 4. This proves the printed booleans come from actually calling into Kratos on this build, and that a wrong claim is caught, rather than the fixture echoing a hard-coded table. Verified: unmutated the fixture PASSES; with T2_MUTATE=1 it FAILS and these expect_in_output strings disappear: all 4 probe[<label>]=<ok>_expected=<must> lines, probe_mismatches=0. Re-verify: cd scripts/tier2_fixtures/kratos/cablenet_spring_mass_matrix_always_lumped && /usr/bin/python3 source.py (passes), then T2_MUTATE=1 /usr/bin/python3 source.py (fails). Interpreter: KRATOS_PYTHON=/usr/bin/python3 is REQUIRED on this host -- the repo venv Kratos wheel is built against GLIBC_2.32 and fails to load (ImportError on Kratos.cpython-312...so), while the system python3.8 carries Kratos 10.4 Release with CableNet, ConstitutiveLaws, StructuralMechanics and LinearSolvers.",
  "backend": "kratos",
  "physics": "cable_net",
  "pitfall_index": 5,
  "mode": "python",
  "expect_in_output": [
    "probe[mass_matrix_has_nonzero_diagonal]=True_expected=True",
    "probe[default_offdiag_is_zero]=True_expected=True",
    "probe[offdiag_still_zero_with_flag_False]=True_expected=True",
    "probe[offdiag_nonzero_with_flag_False]=False_expected=False",
    "probe_mismatches=0"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:"
  ],
  "timeout_seconds": 300
}
