{
  "_comment": "Tier-2: DamApplication thermal laws have a Python CLASS name that differs from the string under which they are registered for Materials.json. Pitfall (kratos.dam #0). Executed on Kratos 10.4.3 (pip wheels, glibc-2.31 host, fresh venv). hasattr(DAM, 'ThermalLinearElastic2DPlaneStrain') is True while HasConstitutiveLaw of that same string is False; the registered strings are ThermalLinearPlaneStrain / ThermalLinearPlaneStress / ThermalElasticIsotropic3D, none of which is a Python attribute. Pick the route first, then the name. The fixture imports KratosMultiphysics for real; with Kratos absent the import raises, the process exits non-zero and the runner records a failure, never a pass. MUTATION CONTROL (added 2026-08-06, re-runnable): T2_MUTATE=1 INVERTS the expected outcome of all 6 probes -- it asserts the opposite of what this build actually does, while leaving each probe's callable untouched so every probe still really runs against Kratos. Every probe[...] line then disagrees with itself and probe_mismatches goes from 0 to 6. This proves the printed booleans come from actually calling into Kratos on this build, and that a wrong claim is caught, rather than the fixture echoing a hard-coded table. Verified: unmutated the fixture PASSES; with T2_MUTATE=1 it FAILS and these expect_in_output strings disappear: all 6 probe[<label>]=<ok>_expected=<must> lines, probe_mismatches=0. Re-verify: cd scripts/tier2_fixtures/kratos/dam_thermal_law_class_vs_registered_string && /usr/bin/python3 source.py (passes), then T2_MUTATE=1 /usr/bin/python3 source.py (fails). Interpreter: KRATOS_PYTHON=/usr/bin/python3 is REQUIRED on this host -- the repo venv Kratos wheel is built against GLIBC_2.32 and fails to load (ImportError on Kratos.cpython-312...so), while the system python3.8 carries Kratos 10.4 Release with CableNet, ConstitutiveLaws, StructuralMechanics and LinearSolvers.",
  "backend": "kratos",
  "physics": "dam",
  "pitfall_index": 0,
  "mode": "python",
  "expect_in_output": [
    "probe[python_attr_ThermalLinearElastic2DPlaneStrain]=True_expected=True",
    "probe[registered_ThermalLinearElastic2DPlaneStrain]=False_expected=False",
    "probe[registered_ThermalLinearPlaneStrain]=True_expected=True",
    "probe[registered_ThermalLinearPlaneStress]=True_expected=True",
    "probe[registered_ThermalElasticIsotropic3D]=True_expected=True",
    "probe[python_attr_ThermalLinearPlaneStrain]=False_expected=False",
    "probe_mismatches=0"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:"
  ]
}
