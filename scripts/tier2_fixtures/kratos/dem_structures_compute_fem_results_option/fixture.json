{
  "_comment": "Tier-2: wall nodal forces stay identically zero unless COMPUTE_FEM_RESULTS_OPTION is enabled — without it ComputeDEMFaceLoadUtility transfers nothing, silently. Pitfall (kratos.dem_structures_coupling #3). Executed on Kratos 10.4.3 (pip wheels, glibc-2.31 host, fresh venv on external ext4 image). The flag resolves on DEMApplication and not on core. DEM_SURFACE_LOAD, the quantity that stays zero, is on DemStructuresCouplingApplication — a THIRD module, neither core nor DEM — so a reader chasing the silent zero has two chances to dot the wrong module before reaching the flag. The fixture imports KratosMultiphysics at module scope; with Kratos absent the import raises, the process exits non-zero and the runner records a failure, never a pass. MUTATION CONTROL (added 2026-08-06, re-runnable): T2_MUTATE=1 INVERTS the expected outcome of all 6 probes -- it asserts the opposite of what this build actually does, while leaving each probe's callable untouched so every probe still really runs against Kratos. Every probe[...] line then disagrees with itself and probe_mismatches goes from 0 to 6. This proves the printed booleans come from actually calling into Kratos on this build, and that a wrong claim is caught, rather than the fixture echoing a hard-coded table. Verified: unmutated the fixture PASSES; with T2_MUTATE=1 it FAILS and these expect_in_output strings disappear: all 6 probe[<label>]=<ok>_expected=<must> lines, probe_mismatches=0. Re-verify: cd scripts/tier2_fixtures/kratos/dem_structures_compute_fem_results_option && /usr/bin/python3 source.py (passes), then T2_MUTATE=1 /usr/bin/python3 source.py (fails). Interpreter: KRATOS_PYTHON=/usr/bin/python3 is REQUIRED on this host -- the repo venv Kratos wheel is built against GLIBC_2.32 and fails to load (ImportError on Kratos.cpython-312...so), while the system python3.8 carries Kratos 10.4 Release with CableNet, ConstitutiveLaws, StructuralMechanics and LinearSolvers.",
  "backend": "kratos",
  "physics": "dem_structures_coupling",
  "pitfall_index": 3,
  "mode": "python",
  "expect_in_output": [
    "probe[DEM.COMPUTE_FEM_RESULTS_OPTION]=True_expected=True",
    "probe[KM.COMPUTE_FEM_RESULTS_OPTION]=False_expected=False",
    "probe[DSC.DEM_SURFACE_LOAD]=True_expected=True",
    "probe[DEM.DEM_SURFACE_LOAD]=False_expected=False",
    "probe[KM.DEM_SURFACE_LOAD]=False_expected=False",
    "probe[DEM.DEM_NODAL_AREA]=True_expected=True",
    "probe_mismatches=0"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:"
  ]
}
