{
  "_comment": "Tier-2: MappingApplication and MeshMovingApplication are required (not optional) for partitioned FSI even when not named in the user-facing JSON, because FSIApplication's partitioned_fsi_base_solver imports both at runtime. Pitfall (kratos.auxiliary_overview #1). NOTE ON THE WRITTEN SIGNAL: this claim carries one of the five shared boilerplate Signal suffixes used across the Kratos catalog, which does not describe what actually happens. The real signal is recorded below and in the fixture output. Executed on Kratos 10.4.3 (pip wheels, glibc-2.31 host, fresh venv on external ext4 image). All four applications the claim names import successfully on this stack, so the dependency is satisfiable; the fixture pins that the two 'optional-looking' applications are real, importable modules rather than aliases, alongside the two obvious ones. The fixture imports KratosMultiphysics at module scope; with Kratos absent the import raises, the process exits non-zero and the runner records a failure, never a pass.",
  "backend": "kratos",
  "physics": "_auxiliary_overview",
  "pitfall_index": 1,
  "mode": "python",
  "expect_in_output": [
    "kratos_version_present=True",
    "importable[FSIApplication]=True_expected=True",
    "importable[MappingApplication]=True_expected=True",
    "importable[MeshMovingApplication]=True_expected=True",
    "importable[FluidDynamicsApplication]=True_expected=True",
    "importable[ContactMechanicsApplication]=False_expected=False",
    "app_import_mismatches=0"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:"
  ],
  "timeout_seconds": 240
}
