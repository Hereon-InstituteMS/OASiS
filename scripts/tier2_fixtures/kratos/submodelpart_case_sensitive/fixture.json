{
  "_comment": "Tier-2 for Kratos linear_elasticity#2: SubModelPart names are case-sensitive. ModelPart.GetSubModelPart('inlet') raises RuntimeError when the actual name is 'Inlet'. The diagnostic comes from ModelPart::ErrorNonExistingSubModelPart in kratos/sources/model_part.cpp. Verified empirically 2026-06-01 — fixture turns the one-shot probe into a regression check. MUTATION CONTROL (added 2026-08-06, re-runnable): T2_MUTATE=1 asks for the sub model part by its actual name \"Inlet\" instead of the lower-case \"inlet\", removing the case mismatch, so the lookup succeeds. Verified: unmutated the fixture PASSES; with T2_MUTATE=1 it FAILS and these expect_in_output strings disappear: RuntimeError, no sub model part with name, model_part. Re-verify: cd scripts/tier2_fixtures/kratos/submodelpart_case_sensitive && /usr/bin/python3 source.py (passes), then T2_MUTATE=1 /usr/bin/python3 source.py (fails). Interpreter: KRATOS_PYTHON=/usr/bin/python3 is REQUIRED on this host -- the repo venv Kratos wheel is built against GLIBC_2.32 and fails to load (ImportError on Kratos.cpython-312...so), while the system python3.8 carries Kratos 10.4 Release with CableNet, ConstitutiveLaws, StructuralMechanics and LinearSolvers.",
  "backend": "kratos",
  "physics": "linear_elasticity",
  "pitfall_index": 2,
  "mode": "python",
  "expect_in_output": [
    "RuntimeError",
    "no sub model part with name",
    "model_part"
  ],
  "forbid_in_output": []
}
