{
  "_comment": "Tier-2: no KratosThermalDEMApplication wheel exists on PyPI. Pitfall (kratos.thermal_dem #1). Executed on Kratos 10.4.3 (pip wheels, glibc-2.31 host, fresh venv on external ext4 image). import KratosMultiphysics.ThermalDEMApplication raises ModuleNotFoundError and pip reports no distribution at any version, while its stated prerequisite DEMApplication imports fine \u2014 so the absence is specific to this wheel. The claim's Signal about an emitted stub script is stale; the generator was removed. The fixture imports KratosMultiphysics at module scope; with Kratos absent the import raises, the process exits non-zero and the runner records a failure, never a pass. INDEX SHIFTED by the guidance reclassification; the claim this fixture verifies is unchanged and every #index quoted above is the current one.",
  "backend": "kratos",
  "physics": "thermal_dem",
  "pitfall_index": 1,
  "mode": "python",
  "expect_in_output": [
    "kratos_version_present=True",
    "importable[ThermalDEMApplication]=False_expected=False",
    "importable[DEMApplication]=True_expected=True",
    "app_import_mismatches=0"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:"
  ],
  "timeout_seconds": 240
}
