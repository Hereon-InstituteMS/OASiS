{
  "_comment": "Tier-2 for Kratos fluid#0: VELOCITY (and other variables) must be added to ModelPart variables list BEFORE any Node is created. Accessing the variable on a Node without prior AddNodalSolutionStepVariable raises RuntimeError 'variables list doesn't have this variable: VELOCITY' from variables_list_data_value_container. Same family as heat#0 (TEMPERATURE). Verified empirically 2026-06-01. MUTATION CONTROL (added 2026-08-06, re-runnable): T2_MUTATE=1 ADDS VELOCITY to the solution-step variable list before the node is created, so GetSolutionStepValue returns a value instead of raising. Verified: unmutated the fixture PASSES; with T2_MUTATE=1 it FAILS and these expect_in_output strings disappear: RuntimeError, variables list, doesn't have this variable. Re-verify: cd scripts/tier2_fixtures/kratos/velocity_variable_not_added && /usr/bin/python3 source.py (passes), then T2_MUTATE=1 /usr/bin/python3 source.py (fails). Interpreter: KRATOS_PYTHON=/usr/bin/python3 is REQUIRED on this host -- the repo venv Kratos wheel is built against GLIBC_2.32 and fails to load (ImportError on Kratos.cpython-312...so), while the system python3.8 carries Kratos 10.4 Release with CableNet, ConstitutiveLaws, StructuralMechanics and LinearSolvers. FIXED 2026-08-08: the expectation 'VELOCITY' was a bare word this fixture writes in its own error arm. Replaced by Kratos's own \"doesn't have this variable:VELOCITY\". SCOPE, recorded rather than repaired: same as temperature_variable_not_added -- the claim is about ADDING BEFORE node creation and neither arm exercises the ordering.",
  "backend": "kratos",
  "physics": "fluid",
  "pitfall_index": 0,
  "mode": "python",
  "expect_in_output": [
    "RuntimeError",
    "variables list",
    "doesn't have this variable",
    "doesn't have this variable:VELOCITY"
  ],
  "forbid_in_output": []
}
