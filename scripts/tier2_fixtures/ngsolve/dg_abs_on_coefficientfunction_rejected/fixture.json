{
  "_comment": "ngsolve dg_methods#9 (Python abs() is not defined on a CoefficientFunction; use IfPos(z,z,-z) or sqrt(z*z)). Run 2026-08-06 on NGSolve 6.2.2604, unit_square maxh=0.25, H1 order 2. All three expression shapes the claim names -- a bare CF, an InnerProduct of two gradients, and a Trace of a 2x2 matrix CF -- raise TypeError with the identical literal text naming ngsolve.fem.CoefficientFunction, so the quoted string is stable across call sites rather than specific to one. Both replacements are then checked to actually be the absolute value: IfPos(z,z,-z) and sqrt(z*z) match math.fabs at five points straddling the sign change, and their integrals over the unit square agree with an independent two-piece integration of the positive and negative parts. Mutation: applying abs() to a float instead of the CF removes the TypeError and the fixture fails on bare_cf_raises. MUTATION CONTROL (re-runnable): T2_MUTATE=1 hands the bare_cf probe a plain Python float (-0.5) instead of the CoefficientFunction z (variable 'bare'), so abs() is applied to a float. Re-verified by execution 2026-08-06 on NGSolve 6.2.2604: the unmutated run matches all 10 expect_in_output strings; T2_MUTATE=1 removes 'bare_cf_raises=True', 'bare_cf_message_literal=True' and 'all_three_shapes_give_same_literal=True' so the fixture goes red. Re-run: T2_MUTATE=1 python source.py",
  "backend": "ngsolve",
  "physics": "dg_methods",
  "pitfall_index": 9,
  "mode": "python",
  "timeout_seconds": 120,
  "expect_in_output": [
    "bare_cf_raises=True",
    "bare_cf_message_literal=True",
    "innerproduct_raises=True",
    "innerproduct_message_literal=True",
    "trace_raises=True",
    "trace_message_literal=True",
    "all_three_shapes_give_same_literal=True",
    "ifpos_is_absolute_value=True",
    "sqrt_is_absolute_value=True",
    "both_replacements_agree_with_split_integral=True"
  ],
  "forbid_in_output": ["Traceback", "FAIL:"]
}
