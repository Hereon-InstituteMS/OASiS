{
  "_comment": "ngsolve dg_methods#0 (L2 needs dgjumps=True or the cross-element coupling entries are never allocated). Run 2026-08-06 on NGSolve 6.2.2604, unit_square maxh=0.3, L2 order 1: the interior-penalty jump form is accepted by BilinearForm.__iadd__ on a plain L2 space and fails only at .Assemble() with 'SparseMatrixTM::AddElementMatrix: illegal dnums' followed by 'in Assemble BilinearForm', exactly where the claim says. The wording the claim declares absent ('Sparse matrix: entry at') really is absent from the message. ndof is identical with and without dgjumps (72); what changes is the allocated sparsity, 216 non-zeros on the plain space against 756 with dgjumps. Mutation: giving the 'plain' space dgjumps=True removes the exception and the fixture fails on plain_assemble_raised. MUTATION CONTROL (re-runnable): T2_MUTATE=1 builds the 'plain' space as L2(mesh, order=1, dgjumps=MUTATE), i.e. with the documented dgjumps=True fix. Re-verified by execution 2026-08-06 on NGSolve 6.2.2604: the unmutated run matches all 8 expect_in_output strings; T2_MUTATE=1 removes 'plain_form_built_without_error=True', 'plain_assemble_raised=True', 'plain_msg_has_illegal_dnums=True', 'plain_msg_has_assemble_tail=True' and 'coupling_entries_allocated=True' so the fixture goes red. Re-run: T2_MUTATE=1 python source.py",
  "backend": "ngsolve",
  "physics": "dg_methods",
  "pitfall_index": 0,
  "mode": "python",
  "timeout_seconds": 120,
  "expect_in_output": [
    "ndof_unchanged_by_dgjumps=True",
    "plain_form_built_without_error=True",
    "plain_assemble_raised=True",
    "plain_msg_has_illegal_dnums=True",
    "plain_msg_has_assemble_tail=True",
    "absent_wording_really_absent=True",
    "dgjumps_assembled=True",
    "coupling_entries_allocated=True"
  ],
  "forbid_in_output": ["Traceback", "FAIL:"]
}
