{
  "_comment": "Tier-2: NGSolve HCurl with grad() bilinear form raises NgException 'SymbolicBFI needs scalar-valued CoefficientFunction' \u2014 grad(HCurl_basis) is vector-tensor, not scalar, so SymbolicBFI rejects it. Verifies an NGSolve-specific failure mode for an electromagnetics user accidentally applying H1-style grad-grad form on HCurl basis. [Re-linked to catalog: index 95 \u2192 8 after maxwell Table-1 promotion 2026-05-31.] MUTATION CONTROL (re-runnable): T2_MUTATE=1 replaces the integrand grad(u)*grad(v)*dx with the H(curl) form curl(u)*curl(v)*dx that the docstring names, so the integrand is scalar-valued and a.Assemble() succeeds. Re-verified by execution 2026-08-06 on NGSolve 6.2.2604: the unmutated run matches all 3 expect_in_output strings; T2_MUTATE=1 removes 'NgException', 'SymbolicBFI' and 'scalar-valued' so the fixture goes red. Re-run: T2_MUTATE=1 python source.py",
  "backend": "ngsolve",
  "physics": "maxwell",
  "pitfall_index": 8,
  "mode": "python",
  "expect_in_output": [
    "NgException",
    "SymbolicBFI",
    "scalar-valued"
  ],
  "forbid_in_output": []
}