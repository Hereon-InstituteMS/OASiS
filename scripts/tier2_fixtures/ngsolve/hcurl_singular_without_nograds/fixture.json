{
  "_comment": "Tier-2 for NGSolve maxwell#0: full HCurl (without nograds=True) has the gradient kernel as null space; the curl-curl matrix is singular and BilinearForm.mat.Inverse raises NgException 'UmfpackInverse: Numeric factorization failed. UMFPACK: WARNING: matrix is singular'. nograds=True or a 1e-8*u*v*dx regularisation removes the kernel and the inverse succeeds. Verified empirically 2026-06-01. MUTATION CONTROL (re-runnable): T2_MUTATE=1 builds the space with nograds=MUTATE, i.e. HCurl(..., nograds=True) -- the documented fix -- on the same unregularised curl-curl form. Re-verified by execution 2026-08-06 on NGSolve 6.2.2604: the unmutated run matches all 3 expect_in_output strings; T2_MUTATE=1 drops ndof from 26 to 13, Inverse() returns normally, no traceback is printed, and 'NgException', 'UmfpackInverse' and 'matrix is singular' are all removed, so the fixture goes red. CAVEAT: this fixture asserts nothing programmatically -- it greps a traceback for three substrings -- and infers the gradient null space from UMFPACK's singularity warning rather than counting it, although the kernel dimension is exactly the 26-13=13 dof difference the nograds flag removes. Re-run: T2_MUTATE=1 python source.py",
  "backend": "ngsolve",
  "physics": "maxwell",
  "pitfall_index": 0,
  "mode": "python",
  "expect_in_output": [
    "NgException",
    "UmfpackInverse",
    "matrix is singular"
  ],
  "forbid_in_output": []
}
