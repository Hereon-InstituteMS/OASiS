{
  "_comment": "Tier-2 for NGSolve plasticity#: catalog claims NewtonCF/MinimizationCF are 'in NGSolve' — they are NOT at the top level. hasattr(ngsolve, 'NewtonCF') is False, 'from ngsolve import *' does NOT expose them. Correct path: from ngsolve.fem import NewtonCF, MinimizationCF. Verified empirically 2026-06-01 (NGSolve 6.2.2604). MUTATION CONTROL (re-runnable, INVERTED POLARITY -- this fixture only ever executes the FIX, so the discriminating edit is to COMMIT the documented mistake): T2_MUTATE=1 replaces 'from ngsolve.fem import MinimizationCF, NewtonCF' with the catalog-implied 'from ngsolve import MinimizationCF, NewtonCF', which raises ImportError (caught, so the report survives and no Traceback is printed) and leaves both names unbound. Re-verified by execution 2026-08-06 on NGSolve 6.2.2604: the unmutated run matches all 6 expect_in_output strings; T2_MUTATE=1 removes 'submodule_NewtonCF_callable=True' and 'submodule_MinimizationCF_callable=True' so the fixture goes red. Re-run: T2_MUTATE=1 python source.py",
  "backend": "ngsolve",
  "physics": "plasticity",
  "pitfall_index": 0,
  "mode": "python",
  "expect_in_output": [
    "top_level_NewtonCF=False",
    "top_level_MinimizationCF=False",
    "star_import_NewtonCF=False",
    "star_import_MinimizationCF=False",
    "submodule_NewtonCF_callable=True",
    "submodule_MinimizationCF_callable=True"
  ],
  "forbid_in_output": [
    "Traceback"
  ]
}
