{
  "_comment": "ngsolve plasticity#2 (return mapping as a per-integration-point Newton, iteration count mesh-independent, quadratic). Observed 2026-08-06 on NGSolve 6.2.2604 with power-law hardening and ngsolve.fem.NewtonCF on meshes of 14/54/224 elements: the smallest maxiter giving a non-NaN result is the same on all three, the residual history r_k+1/r_k^2 stays near 2e-5 (quadratic), and after convergence the area fraction with |sigma_vm - Y| > 1e-10 is exactly 0.0 on every mesh. Under-iterating NewtonCF returns NaN, its documented failure signal.",
  "backend": "ngsolve",
  "physics": "plasticity",
  "pitfall_index": 2,
  "mode": "python",
  "timeout_seconds": 180,
  "expect_in_output": [
    "newtoncf_module=ngsolve.fem",
    "newtoncf_doc_says_nan_on_failure=True",
    "trial_state_is_plastic_everywhere=True",
    "consistency_below_1e-10_everywhere=True",
    "min_maxiter_identical_across_meshes=True",
    "min_maxiter_bounded_le_10=True",
    "local_newton_quadratic=True",
    "local_residual_reaches_machine_precision=True",
    "returns_nan=True",
    "elastic_trial_violates_yield_everywhere=True"
  ],
  "forbid_in_output": ["Traceback", "FAIL:"]
}
