{
  "_comment": "Tier-2 for skfem linear_elasticity#2, previously flagged 'claim inherited -- not yet empirically separated'. Now measured on skfem 12.0.1: linear_elasticity(lam, mu) IS a skfem.assembly.form.BilinearForm instance (not an integrand), asm() returns an (N, N) scipy sparse matrix on a vector basis, and calling it on a SCALAR basis raises rather than silently assembling. MUTATION CONTROL (re-runnable): T2_MUTATE=1 applies the documented fix at wrong-variant (b) -- the element handed to that asm call is wrapped as ElementVector(ElementQuad1()), giving elasticity the vector space it needs, so the call assembles instead of raising. Re-verified by execution 2026-08-06 on skfem 12.0.1 / NGSolve 6.2.2604: the unmutated run matches all 5 expect_in_output strings; T2_MUTATE=1 removes 'scalar_basis_raises=True' and makes the forbidden 'FAIL:' appear, so the fixture goes red. Re-run: T2_MUTATE=1 python source.py",
  "backend": "skfem",
  "physics": "linear_elasticity",
  "pitfall_index": 2,
  "mode": "python",
  "expect_in_output": [
    "is_bilinearform_instance=True",
    "asm_shape_equals_basis_N=True",
    "asm_returns_scipy_sparse=True",
    "scalar_basis_raises=True",
    "calling_form_as_integrand_raises=True"
  ],
  "forbid_in_output": [
    "Traceback",
    "FAIL:"
  ]
}
